{
  "name": "Health and Prosperity Index",
  "build": {
    "dockerfile": "Dockerfile",
    "context": "."
  },
  "settings": {
    "terminal.integrated.defaultProfile.linux": "/bin/bash"
  },
  "extensions": [
    "ms-python.python"
  ],
  "forwardPorts": [8501],
  "postCreateCommand": "pip install -r requirements.txt",
  "remoteUser": "root"
}
